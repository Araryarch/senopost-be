meta {
  name: Delete User
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/users/:id
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}

docs {
  Delete user (requires auth).
}
