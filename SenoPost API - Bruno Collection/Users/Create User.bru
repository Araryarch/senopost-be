meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{base_url}}/users
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "email": "newuser@example.com",
    "password": "password123"
  }
}

settings {
  encodeUrl: true
}

docs {
  Create user (admin style) - this endpoint exists in API but may be similar to auth/register.
}
